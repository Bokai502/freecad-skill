interface:
  display_name: "FreeCAD"
  short_description: "Run FreeCAD layout CLI workflows"
  default_prompt: "Use $freecad to inspect runtime/workspace config, build layout or component-info assemblies, safely move components, and verify STEP/GLB workflow outputs."
